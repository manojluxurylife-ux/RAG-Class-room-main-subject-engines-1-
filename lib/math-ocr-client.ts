export async function requestMathOcr(payload:any){const endpoint=process.env.MATH_OCR_WORKER_URL?.trim();if(!endpoint)return{latex:"",confidence:0,engine:"disabled"};const r=await fetch(`${endpoint.replace(/\/$/,"")}/v1/equation`,{method:"POST",headers:{"Content-Type":"application/json",...(process.env.MATH_OCR_SHARED_SECRET?{"X-AI-Guru-Secret":process.env.MATH_OCR_SHARED_SECRET}:{})},body:JSON.stringify(payload),signal:AbortSignal.timeout(45000)});if(!r.ok)throw Error(`Math OCR worker failed (${r.status})`);return r.json();}
